import { Typography, Box } from "@mui/material";

function Publish() {
  return (
    <Box sx={{ textAlign: "center", mt: 4 }}>
      <Typography variant="h4">Publish Your Blog</Typography>
      <Typography variant="body1" mt={2}>
        This page is under construction.
      </Typography>
    </Box>
  );
}

export default Publish;